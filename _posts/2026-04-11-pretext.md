---
layout: post
title: Pretext 적용
subtitle: 줄 수 세려고 리플로우 부르지 말자
date: 2026-04-11 00:00:00 +0900
sitemap :
  changefreq : daily
  priority : 1.0
---

요즘 이 블로그를 좀 덜 굼뜨게 만들고 싶었다.
글 하나 띄울 때마다 브라우저한테 이거 몇 줄임 하고 계속 물어보면, 당연히 레이아웃이 한 번씩 깨워진다.
한두 덩어리면 티가 덜 나는데 문단이 좀 쌓이고 폭이 바뀌기 시작하면 바로 귀찮아진다.
그래서 Pretext를 붙였다.

Pretext는 텍스트를 미리 쪼개서 폭을 준비해두고, 실제 줄바꿈이랑 높이 계산은 수학으로만 다시 돌리는 JS 라이브러리다.
핵심은 DOM 박스를 다시 재지 않는다는 점이다.
`getBoundingClientRect()` 같은 걸로 레이아웃을 흔들지 않고, 준비된 세그먼트 폭만 들고 바로 몇 줄인지 계산한다.
이 블로그처럼 텍스트 덩어리가 많은 곳에는 이런 방식이 꽤 잘 맞는다.

지금 포스트 페이지도 마크다운을 읽은 다음 텍스트를 다시 배치하는 흐름이 들어가 있다.
여기서 문단마다 DOM 측정까지 섞어버리면 그냥 스스로 발목 잡는 셈이다.
반대로 한 번 `prepareWithSegments()` 해두면, 폭이 바뀔 때는 `layoutWithLines()`만 다시 태우면 된다.
말 그대로 줄바꿈을 브라우저 눈치 보지 말고 계산으로 처리하자는 쪽이다.

아래 데모가 그걸 그대로 보여준다.
문단 폭이 숨 쉬듯 늘었다 줄었다 하고, 슬라이더를 직접 끌어도 된다.
그때마다 줄 수랑 측정 높이가 바로 바뀌는데, 저 숫자는 DOM 박스 재서 나온 값이 아니다.
Pretext가 준비해둔 값으로 다시 접은 결과다.

<div id="pretext-demo" style="margin: 2rem 0; border: 1px solid var(--bs-border-color, rgba(127,127,127,0.25)); border-radius: 18px; background: linear-gradient(180deg, color-mix(in srgb, var(--bs-body-bg, #fff) 94%, var(--bs-emphasis-color, #000) 6%), color-mix(in srgb, var(--bs-body-bg, #fff) 98%, var(--bs-emphasis-color, #000) 2%)); overflow: hidden; box-shadow: 0 18px 36px rgba(0,0,0,0.08);">
  <template id="pretext-demo-src"><!doctype html><html lang="ko"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><style>:root{color-scheme:light;--bg:rgba(255,255,255,.92);--panel:rgba(255,255,255,.78);--panel-strong:rgba(255,255,255,.94);--line:rgba(15,23,42,.12);--text:#111827;--muted:#6b7280;--accent:#2563eb;--accent-soft:rgba(37,99,235,.08);--track:rgba(148,163,184,.22)}:root[data-theme="dark"]{color-scheme:dark;--bg:rgba(12,18,30,.88);--panel:rgba(15,23,42,.74);--panel-strong:rgba(15,23,42,.9);--line:rgba(148,163,184,.18);--text:#e5e7eb;--muted:#9ca3af;--accent:#7dd3fc;--accent-soft:rgba(125,211,252,.12);--track:rgba(148,163,184,.26)}*{box-sizing:border-box}html,body{margin:0;padding:0;background:transparent}body{font-family:"Pretendard Variable",-apple-system,BlinkMacSystemFont,sans-serif;color:var(--text)}.demo-shell{width:min(100%,700px);margin:0 auto;padding:18px;background:linear-gradient(180deg,var(--bg),color-mix(in srgb,var(--bg) 86%,transparent));border:1px solid transparent}.demo-header{display:grid;gap:12px;margin-bottom:18px}.demo-title-wrap{display:grid;gap:6px}.demo-title{margin:0;font-size:14px;font-weight:700;letter-spacing:-.02em}.demo-copy{margin:0;font-size:12px;line-height:1.55;color:var(--muted)}.control-card,.panel{border:1px solid var(--line);border-radius:16px;background:linear-gradient(180deg,var(--panel-strong),var(--panel));box-shadow:0 10px 24px rgba(15,23,42,.06)}.control-card{padding:14px 16px;margin-bottom:14px}.control-top{display:flex;justify-content:space-between;gap:12px;align-items:flex-end;margin-bottom:10px}.control-label{font-size:12px;font-weight:600;letter-spacing:-.01em}.control-hint{font-size:12px;color:var(--muted)}input[type=range]{width:100%;margin:0;accent-color:var(--accent)}.panels{display:grid;gap:14px}.panel{padding:14px 16px}.panel-head{display:flex;align-items:center;justify-content:space-between;gap:12px;margin-bottom:12px}.panel-label{font-size:12px;font-weight:700;letter-spacing:.02em;text-transform:uppercase}.panel-note{font-size:11px;color:var(--muted)}.surface{width:100%;padding:14px 16px;border-radius:14px;border:1px solid var(--line);background:linear-gradient(180deg,color-mix(in srgb,var(--panel-strong) 92%,transparent),color-mix(in srgb,var(--panel) 96%,transparent));box-shadow:inset 0 1px 0 rgba(255,255,255,.08)}.surface-inner{width:100%;margin:0 auto}.line{display:block;white-space:pre-wrap;line-height:1.75;font-size:15px;letter-spacing:-.01em}.css-copy{margin:0;white-space:normal;overflow-wrap:break-word;word-wrap:break-word;line-height:1.75;font-size:15px;letter-spacing:-.01em}.divider{height:1px;background:linear-gradient(90deg,transparent,var(--line),transparent);margin:8px 0 2px}.sr-only{position:absolute;width:1px;height:1px;padding:0;margin:-1px;overflow:hidden;clip:rect(0,0,0,0);white-space:nowrap;border:0}</style></head><body><div class="demo-shell"><div class="demo-header"><div class="demo-title-wrap"><h2 class="demo-title">같은 문단을 두 방식으로 접어보기</h2><p class="demo-copy">슬라이더 하나로 같은 폭을 동시에 바꾸면, CSS의 기본 줄바꿈과 Pretext가 계산한 줄 배치가 어떻게 보이는지 바로 비교할 수 있다.</p></div></div><div class="control-card"><div class="control-top"><label class="control-label" for="width-control">문단 폭</label><span class="control-hint">두 블록이 같은 폭으로 함께 바뀝니다</span></div><input id="width-control" type="range" min="220" max="560" step="1" value="360"></div><div class="panels"><section class="panel" aria-labelledby="pretext-panel-label"><div class="panel-head"><div class="panel-label" id="pretext-panel-label">Pretext</div><div class="panel-note">prepareWithSegments → layoutWithLines</div></div><div class="surface"><div id="pretext-box" class="surface-inner" aria-live="polite"></div></div></section><section class="panel" aria-labelledby="css-panel-label"><div class="panel-head"><div class="panel-label" id="css-panel-label">CSS word-wrap</div><div class="panel-note">브라우저 기본 줄바꿈</div></div><div class="surface"><div id="css-box" class="surface-inner"><p id="css-copy" class="css-copy"></p></div></div></section></div></div><script type="module">import { prepareWithSegments, layoutWithLines } from '/assets/js/pretext/layout.js'; const text = '폭이 조금만 바뀌어도 문단은 금방 다른 모양이 된다. 브라우저에게 지금 몇 줄인지 계속 다시 묻는 대신, Pretext는 미리 준비한 세그먼트 폭으로 같은 문장을 다시 접는다. 그래서 리플로우를 깨우지 않고도 줄 수와 높이를 빠르게 계산할 수 있다.'; const lineHeight = 26.25; const slider = document.getElementById('width-control'); const pretextBox = document.getElementById('pretext-box'); const cssBox = document.getElementById('css-box'); const cssCopy = document.getElementById('css-copy'); cssCopy.textContent = text; function getParentTheme(){ try { const root = window.parent?.document?.documentElement; const theme = root?.getAttribute('data-bs-theme'); if (theme === 'dark' || theme === 'light') return theme; } catch {} return window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light'; } function applyTheme(){ document.documentElement.dataset.theme = getParentTheme(); } applyTheme(); try { const parentRoot = window.parent?.document?.documentElement; if (parentRoot) { new MutationObserver(applyTheme).observe(parentRoot,{attributes:true,attributeFilter:['data-bs-theme']}); } } catch {} window.matchMedia('(prefers-color-scheme: dark)').addEventListener?.('change', applyTheme); const font = '400 15px "Pretendard Variable", -apple-system, BlinkMacSystemFont, sans-serif'; const prepared = prepareWithSegments(text, font); function postHeight(){ const height = Math.ceil(document.documentElement.scrollHeight); window.parent.postMessage({ type:'pretext-demo-height', height }, '*'); } function render(width){ pretextBox.style.width = width + 'px'; cssBox.style.width = width + 'px'; const layout = layoutWithLines(prepared, width, lineHeight); pretextBox.innerHTML = ''; layout.lines.forEach((line)=>{ const row = document.createElement('span'); row.className = 'line'; row.textContent = line.text; pretextBox.appendChild(row); }); requestAnimationFrame(postHeight); } slider.addEventListener('input', ()=> render(Number(slider.value))); window.addEventListener('load', ()=> render(Number(slider.value))); window.addEventListener('resize', ()=> render(Number(slider.value))); render(Number(slider.value));</script></body></html></template>
  <iframe title="Pretext text measurement demo" style="width: 100%; height: 1px; border: 0; background: transparent; display: block; overflow: hidden;" sandbox="allow-scripts allow-same-origin" onload="(function(frame){if(!frame.dataset.bound){frame.dataset.bound='1';window.addEventListener('message',function(event){if(event.source!==frame.contentWindow||!event.data||event.data.type!=='pretext-demo-height'){return;}frame.style.height=Math.max(1,Number(event.data.height)||1)+'px';});}frame.removeAttribute('onload');frame.srcdoc=frame.previousElementSibling.innerHTML;})(this)"></iframe>
</div>

이런 건 화려하진 않은데 한 번 깔아두면 꽤 속이 편하다.
텍스트가 많은 페이지에서 레이아웃 읽기랑 쓰기가 서로 싸우는 꼴을 덜 보게 되기 때문이다.
블로그에 이런 걸 왜 넣냐고 하면, 그냥 넣고 싶었고 넣을 만했다.
